{
  "name": "openfiat-apps",
  "version": "0.1.0",
  "private": true,
  "description": "OpenFiat applications: wallet, merchant dashboard, and network explorer.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build"
  }
}
